import { CliLearnClient } from "../learn-client";

export const metadata = {
  title: "CLI Learning Path — SolStudio Docs",
};

export default function CliLearnPage() {
  return <CliLearnClient />;
}
