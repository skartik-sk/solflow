import { CloudLearnClient } from "../learn-client";

export const metadata = {
  title: "Cloud Learning Path — SolStudio Docs",
};

export default function CloudLearnPage() {
  return <CloudLearnClient />;
}
