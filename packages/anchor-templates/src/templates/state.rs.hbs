use anchor_lang::prelude::*;

#[account]
#[derive(InitSpace)]
pub struct {{name}} {
    {{#each fields}}
    {{#if description}}
    /// {{description}}
    {{/if}}
    pub {{name}}: {{rustType}},{{#if sizeComment}}  // {{sizeComment}}{{/if}}
    {{/each}}
}
